export const API_URL: string = process.env.NODE_ENV === 'development' ? '' : '' // api请求地址
export const REQUEST_TIME_OUT: number = 10000 // 默认请求超时时间
export const RESPONSE_DELAY: number = 1000 // mock请求延时
